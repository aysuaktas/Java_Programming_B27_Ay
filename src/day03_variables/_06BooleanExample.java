package day03_variables;

public class _06BooleanExample {
    public static void main(String[] args) {

        boolean isSleepy = true;   // (( I am hungry now, true ))
        boolean isHungry = true;
        boolean isWeekend = false; // (( today is not weekend, so it's false ))

        System.out.println("isSleepy = " + isSleepy);
        System.out.println("isHungry = " + isHungry);
        System.out.println("isWeekend = " + isWeekend);

        int Public = 40; // valid name, because Java is case sensitive, so uppercase and lowercase are different -> public is not equal to Public. ((rules for variable names konusundan bahsederken degindik))

    }

}
