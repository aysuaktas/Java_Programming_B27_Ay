package practices_group_15;

public class _11DailyQuestion {

    /*
    // What will happen if you do not initialize an array?


    There are two ways when we declare an array:
        1) We can create an array with initial values (if we already know the elements). For ex: String[] words = {"daily", "question", "java"};
        2) We can create an empty array with certain size (which is amount of elements).  For ex: String[] words = new String [3];

        If we create an array with this second way (with empty array), there are default values based on the type:

                Default values:
                  int     -> 0
                  double  -> 0.0
                  boolean -> false
                  object  -> null (means: there is no object, nothing)


        ---------------------------------
        David's answer:

        The array will take default values depending upon the data type.

        0  -> for byte
                  short
                  int
                  long

        0.0 -> for float
                   double

        false -> for boolean

        null -> for objects


        ----------------------------------

        KENDIME NOT;;;;

        Array Initialization:

	Like regular variables, Java also allows you to initialize an array’s elements when you create the array:

	int[ ] myList = {1,2,3,4,5};

	Bu sayede hem declare hem assign etmis olduk.









     */

}
