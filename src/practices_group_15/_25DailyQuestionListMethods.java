package practices_group_15;

public class _25DailyQuestionListMethods {

    /*
    About below List methods, which options have correct combination of parameters and return type?(more than 1 correct answers):

        A. get :
            - parameter: int
            - returnType: object

        B. add:
            - parameter: object
            - return type: boolean

        C. addAll:
            - parameter: object
            - returnType: boolean

        D. indexOf:
            - parameter: object
            - returnType: int
     */


    /*

    Merve's answer is:

    A,B,D
    for addAll method; the parameter should be collection type

     */


    /* Benim cevabim:

    Answer is: A, B, D.

     A. get() method returns the element at the given index number, so its parameter is int, and its returnType is object.

        B. add() method adds the element to the end of the ArrayList, so its parameter is object. Its return type is boolean because it will return true or false.

        C. addAll() is a bulk operation method. Its parameter is collection type. It adds all the elements from the given argument to the ArrayList. Its returnType is boolean.


        D. indexOf() method returns the index of the given element. So, its parameter is object and its returnType is int.

     */

}
